// firebase-config.js
// IMPORTANT: Replace the placeholder values with your Firebase project's config.
// You can find this in your Firebase console -> Project settings -> SDK setup and configuration.
const FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// Instance ID for the document where app state is stored. Change if you want separate workspaces.
const FIREBASE_INSTANCE_ID = "default-instance";

